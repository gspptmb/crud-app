{
  "name": "crud-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start:dev": " JWT=shh nodemon src/handlers/server.js",
    "make-model": "npx sequelize-cli model:generate --name User --attributes username:string,password:string",
    "migrate": "npx sequelize-cli db:migrate",
    "down": "npx sequelize-cli db:migrate:undo",
    "make-seeder": "npx sequelize-cli seed:generate --name demo-user",
    "seeder": "npx sequelize-cli db:seed:all",
    "seeder-down": "npx sequelize-cli db:seed:undo",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "prettier": {
    "singleQuote": true,
    "tabWidth": 4
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^8.5.1",
    "pg": "^8.8.0",
    "sequelize": "^6.24.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "sequelize-cli": "^6.5.1"
  }
}